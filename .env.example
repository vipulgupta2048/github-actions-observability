# Environment variables for GitHub receiver
# Copy this file to .env and fill in your actual values

# GitHub Personal Access Token (classic)
# Generate at: https://github.com/settings/tokens
# Required scopes: metadata:read, actions:read, contents:read
GITHUB_TOKEN=your_github_token_here

# Webhook Secret (random 32+ character string)
# Generate with: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# GitHub Organization and Repository
GITHUB_ORG=your_github_org
GITHUB_REPO=your_repo_name

# Optional: Custom tunnel URL (if using cloudflared)
# TUNNEL_URL=https://your-tunnel-url.trycloudflare.com

# Optional: Git branch for webhook testing
# GITHUB_REF=main